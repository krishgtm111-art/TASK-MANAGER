// Base URL of the backend. Adjust via a .env file (VITE_API_URL=...) if the
// backend runs somewhere other than the Vite dev default.
export const API_BASE_URL = import.meta.env.VITE_API_URL || "http://localhost:3000"
